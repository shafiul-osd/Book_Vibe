import React from 'react';

const PagesToRead = () => {
    return (
        <div>
            <h2>this is the pages to read components and it have to read the book section</h2>
        </div>
    );
};

export default PagesToRead;